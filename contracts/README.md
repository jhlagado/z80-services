# Contract authority

`z80-services-v0.json` is the numeric authority for the experimental contract.
Run `deno task generate` after changing it. The generated TypeScript and ATOM
files must be committed with the manifest change.

Version zero records the synchronous byte gateway already used in current
proofs. Its names and numbers are provisional until host and native
conformance agree and at least two consumers qualify the same behaviour.
